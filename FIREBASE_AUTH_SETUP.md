# إعداد Firebase Authentication لنظام إدارة العقود

## خطوات إعداد المصادقة:

### 1. تفعيل Authentication في Firebase Console:
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك (`qanuni-499f2`)
3. من القائمة الجانبية، اختر **Authentication**
4. اضغط على **Get started**
5. اذهب إلى تبويب **Sign-in method**
6. فعّل **Email/Password** كطريقة تسجيل دخول

### 2. إعداد المستخدم الأول (المدير):

#### الطريقة الأولى - من خلال التطبيق:
1. شغّل التطبيق
2. في شاشة تسجيل الدخول، اضغط على **"إنشاء حساب"**
3. أدخل إيميل المدير وكلمة مرور قوية
4. اضغط **"إنشاء الحساب"**

#### الطريقة الثانية - من خلال Firebase Console:
1. في Firebase Console → Authentication → Users
2. اضغط **Add user**
3. أدخل إيميل المدير وكلمة المرور
4. اضغط **Add user**

### 3. قواعد الأمان (Security Rules):

#### Firestore Rules:
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /legal_contracts/{document} {
      // السماح بالقراءة والكتابة للمستخدمين المصادق عليهم فقط
      allow read, write: if request.auth != null;
    }
  }
}
```

#### Storage Rules:
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /legal_contracts/{allPaths=**} {
      // السماح بالقراءة والكتابة للمستخدمين المصادق عليهم فقط
      allow read, write: if request.auth != null;
    }
  }
}
```

### 4. الميزات المتوفرة:

#### 🔐 **شاشة تسجيل الدخول:**
- تسجيل دخول بالإيميل وكلمة المرور
- إنشاء حساب مدير جديد
- إعادة تعيين كلمة المرور
- تصميم جميل ومتجاوب

#### 👤 **إدارة المستخدم:**
- عرض معلومات الحساب
- تغيير كلمة المرور
- تسجيل الخروج الآمن
- قائمة إعدادات في AppBar

#### 🛡️ **الأمان:**
- حماية كاملة للشاشة الرئيسية
- التحقق من صحة البيانات
- رسائل خطأ مفصلة بالعربية
- إدارة حالة المصادقة تلقائياً

### 5. استخدام النظام:

#### تسجيل الدخول:
1. افتح التطبيق
2. أدخل إيميل المدير وكلمة المرور
3. اضغط **"تسجيل الدخول"**

#### إعدادات المدير:
1. اضغط على أيقونة المدير في الأعلى
2. اختر من القائمة:
   - **معلومات الحساب**: عرض تفاصيل الحساب
   - **تغيير كلمة المرور**: تغيير كلمة المرور
   - **تسجيل الخروج**: خروج آمن من النظام

### 6. نصائح أمنية:

#### كلمات المرور:
- استخدم كلمة مرور قوية (أكثر من 8 أحرف)
- امزج بين الأحرف والأرقام والرموز
- لا تشارك كلمة المرور مع أحد

#### الحساب:
- فعّل التحقق بخطوتين في Firebase Console
- راقب نشاط تسجيل الدخول
- غيّر كلمة المرور دورياً

### 7. استكشاف الأخطاء:

#### مشاكل شائعة:
- **"لا يوجد مستخدم"**: تأكد من صحة الإيميل
- **"كلمة مرور خاطئة"**: تحقق من كلمة المرور
- **"خطأ شبكة"**: تحقق من الاتصال بالإنترنت

#### حلول:
- استخدم **"نسيت كلمة المرور"** لإعادة التعيين
- تحقق من إعدادات Firebase في Console
- راجع قواعد الأمان (Security Rules)

## الوضع الحالي: ✅ مكتمل وجاهز للاستخدام

النظام محمي بالكامل ويتطلب تسجيل دخول للوصول إلى ميزات إدارة العقود.
